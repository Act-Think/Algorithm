// var input = require('fs').readFileSync('/dev/stdin').toString();

var input = "Mississsipi"
var inputUp = input.toUpperCase();
var az = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('');
var zero = new Array(26).fill(0);

for (i=0; i<inputUp.length; i++) {
    if (inputUp[i] === 
}

